<!-- Components/Board -->
@using Components
@using Models

@code{
    public Models.Board board = new Models.Board();
}

@board = <div class="board comp" onload="@board">
   <Row @on /> 
   <Row />
   <Row />
   <Row />
   <Row />
   <Row />
</div>

<style scoped>
    .board {
        margin: 0 auto;
        display: grid;
        grid-auto-rows:auto;
        background-color: rgba(19, 19, 19, 0.79);
        padding: 0;
        /* in px so it doesn't scale */
        width: 400px;
        height: 400px;
    }
</style>
