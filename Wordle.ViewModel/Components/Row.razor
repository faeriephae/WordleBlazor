<!-- Components/Rows -->
@using Components
@using Wordle.Model
@using Wordle.ViewModel.Classes

@inject GameManager gameManager

<div class="row comp">    
    @for(int i = 0; i < gameManager.NumberColumns; i++)
    {
        <Square RowIndex=@RowIndex ColumnIndex=@i/>
    }
@*
    @for(int i = 0; i < 5; i++)
    {
        int sqNumber = i;
        <Square @key=sqNumber @onkeydown=KeyDown/>
    }*@
</div>

<style scoped>
/*    .row {
        margin: 0 auto;
        background-color: inherit;
        width: 100%;
        height: 100%;
    }*/
</style>

@code {
    [Parameter]
    public int RowIndex { get; set; }
    //public char value { get; set; }

    //private int currSq = 0;

    //private char[] values = new char[30];

    //private void KeyDown(KeyboardEventArgs e)
    //{
    //    values[currSq] = Convert.ToChar(e.Key);
    //    currSq++;
    //}
}
